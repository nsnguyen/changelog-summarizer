{
  "name": "changelog-summarizer",
  "version": "1.0.0",
  "description": "A GitHub Action to summarize commits and generate changelogs using ChatGPT",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js --license licenses.txt",
    "test": "node src/index.js"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@actions/exec": "^1.1.1",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}